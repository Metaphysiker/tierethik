//console.log("load slide");
var field_id_name = "#debate_field";

$(field_id_name).fadeOut("fast", function(){
  $(field_id_name).empty().append(
    `
    <%= render "slides/show", slide: @slide unless @slide.blank? %>
    `
  ).fadeIn("fast");
});
