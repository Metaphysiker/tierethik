<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to root_path, class: "navbar-brand text-h-color" do %>
      Fokus Tierethik
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to "Blog", posts_path, class:"nav-link #{'active' unless @posts.nil? && @post.nil?}"  %>
      </li>
      <li class="nav-item">
        <%= link_to 'Events', events_path, class:"nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Argumentiere gegen einen Veganer!", debate_a_vegan_path, class:"nav-link"  %>
      </li>
      <% Page.all.each do |page| %>
        <li class="nav-item">
          <%= link_to page.title, page_path(page), class:"nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
