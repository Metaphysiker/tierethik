<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to root_path, class: "navbar-brand text-h-color" do %>
      Fokus Tierethik
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownBlogLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Blog
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownBlogLink">
          <%= link_to "Alle", posts_path, class:"dropdown-item"  %>
          <% Tag.tags.each do |tag| %>
            <%= link_to tag, posts_path(tag: tag), class:"dropdown-item" %>
          <% end %>
        </div>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownEventLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Events
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownEventLink">
          <%= link_to 'Kalender', event_calendar_path, class:"dropdown-item" %>
          <%= link_to 'Aktionslandkarte', action_map_path, class:"dropdown-item" %>
          <%= link_to 'Event vorschlagen', new_event_path, class:"dropdown-item" %>
        </div>
      </li>
      <li class="nav-item">
        <%= link_to "Argumentiere mit einem Veganer!", debate_a_vegan_path, class:"nav-link"  %>
      </li>
      <% Page.all.each do |page| %>
        <li class="nav-item">
          <%= link_to page.title, page_path(page), class:"nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
